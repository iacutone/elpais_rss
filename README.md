# ElpaisRss

Backend for [Master Spanish Today](https://masterspanish.today)

## Installation

If [available in Hex](https://hex.pm/docs/publish), the package can be installed
by adding `elpais_rss` to your list of dependencies in `mix.exs`:

```elixir
def deps do
  [
    {:elpais_rss, "~> 0.1.0"}
  ]
end
```
